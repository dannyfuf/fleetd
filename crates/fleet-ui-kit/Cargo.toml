[package]
name = "fleet-ui-kit"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Fleet design system: tokens, theme, embedded Lucide icons and the component API contract, on raw gpui."

[dependencies]
gpui.workspace = true

[dev-dependencies]
gpui_platform.workspace = true

[[example]]
name = "kit_gallery"
path = "examples/kit_gallery.rs"
