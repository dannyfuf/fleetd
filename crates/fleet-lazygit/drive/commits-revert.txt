# Commits pane: `t` on row 1 (HEAD, c4) then `enter`.
# Expected: a fifth commit whose subject starts with `Revert` and gamma.txt gone.
wait 1500
key 4
wait 400
key <
wait 300
key t
wait 500
key enter
wait 3000
quit
