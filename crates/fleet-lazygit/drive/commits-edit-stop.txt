# Commits pane: `e` on row 2 (c3) only. Expected: the app is left in REBASING
# with HEAD detached at c3 and `.git/rebase-merge` present.
wait 1500
key 4
wait 400
key <
wait 200
key j
wait 300
key e
wait 3000
quit
