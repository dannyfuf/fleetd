# Branches pane: `r` on `main` (row 2, since the checked-out `feature` is row 1)
# then `enter`. Expected: feature's commits are replayed on top of main's tip, so
# `main` becomes an ancestor of `feature`.
wait 1500
key 3
wait 300
key <
wait 200
key j
wait 300
key r
wait 500
key enter
wait 3500
quit
