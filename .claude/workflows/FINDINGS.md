# Quality pass — working notes

Temporary. Tracks the chore/cleaning quality pass: what the audit found, what was fixed,
and what was deliberately left. Deleted together with `quality-pass.js` when the pass lands.

## Status

- [x] Audit workflow authored (`quality-pass.js`)
- [ ] Audit run
- [ ] Findings triaged
- [ ] Fixes landed

## Findings

_(populated by the audit run)_
