# Situation

An earlier agent started fixing the verified findings in `.claude/workflows/state/batches/w1-server.json` and was interrupted partway
through. **Its partial, unreviewed work is already committed** — it is in the files at HEAD, not
in `git diff`. So `git diff` being empty tells you nothing; read the *current code*.

`cargo check --workspace --all-targets` passes. The full suite was 794 passed / 1 failed and then
aborted, so most of it is unverified. Nothing in that partial work has been confirmed correct.

Your job is to finish this batch and make it trustworthy.

# Task

1. Read `.claude/workflows/state/batches/w1-server.json` — JSON, one entry per defect that two independent agents already established.
   `summary` + `evidence` state it, `verify_reason` is a second reader's confirmation (read it;
   it often corrects the first reader), `proposed_fix` / `corrected_fix` say how to fix it
   (prefer `corrected_fix`), `blast_radius` names what else it touches, `test_plan` names the
   test. To see what the interrupted agent did to a file, use
   `git log -p --since=4.hours -- <path>` or diff against `git show c6fabf8:<path>` (the commit
   before its work).
2. For each finding, read the current code and decide honestly which it is:
   - **already fixed and covered by a test that genuinely fails without the fix** → confirm and move on;
   - **partially or wrongly done** → correct it;
   - **untouched** → do it;
   - **not a real defect** → back that part out and report `no_change_needed` with your reason.
     That is a valid, useful outcome.
3. Trust no fix you did not verify and no test you did not watch run.

# Files you own

Create and edit ONLY these paths:
  - crates/fleet-daemon/src/server/**
  - crates/fleet-daemon/tests/server_events.rs
  - crates/fleet-daemon/tests/remote_lifecycle.rs
  - crates/fleet-client/**
  - crates/fleet-app/src/shell/daemon.rs
  - docs/APP-CONTRACTS.md

Other agents are editing other parts of this same tree concurrently:
- Never edit a file outside that list. If a fix needs one, do the part you can and report the rest.
- `cargo check` / `cargo test` may show errors in files you do not own — they belong to another
  agent. Ignore them; never fix or revert them.
- Cargo may block on the target-directory lock. Wait. Do not kill it or set your own CARGO_TARGET_DIR.
- Never run `git add`, `git commit`, `git checkout`, `git stash`, `git restore`, or `git clean`.
  To undo something in a file you own, edit the file.

# How to work

1. Read the `.claude/skills/` skills for your area — rust-ipc-protocol, rust-async-background-work, rust-gpui-testing — and their `references/checklist.md`.
   They are this repo's engineering bar and what your work is reviewed against. `CLAUDE.md`'s
   non-negotiables are binding. `docs/` is authoritative: if code and a doc disagree, both move.
2. Findings in severity order: high, then medium, then low.
3. **Every behavioural fix needs a regression test that fails without it.** Where a test already
   exists, prove it: temporarily revert the fix, watch it fail, restore, watch it pass. A test
   nobody watched fail is not a regression test.
4. Match the surrounding code exactly — its helpers, its fake/adapter seams, its test layout
   (inline `mod tests`, `src/<thing>/tests.rs`, `crates/<crate>/tests/` — copy the neighbours),
   its comment density, its error idiom.
5. Minimal and surgical. No drive-by refactors, renames, reformatting, new features, or widened
   public API the fix does not need.
6. Finish with `cargo test -p <crate> <filter>` and `cargo check -p <crate> --all-targets`, for real.

# Report

One line per finding id — `fixed` / `skipped` / `no_change_needed` — the final state of the code
with `file:line`, which test covers it, and whether you personally watched that test fail first.
Then every file you touched, and anything left undone.

Accurate over optimistic: everything is checked against the diff. Say plainly what you could not
verify rather than implying you did.

Your batch is **w1-server**.